name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-24.04
    steps:
      - name: Print greeting
        run: |
          echo "First output"
          echo "Second output"
      - name: Print goodbye
        run: echo "Done - bye!"
